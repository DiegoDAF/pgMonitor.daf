services:
  pgscv-disrupt-ro:
    image: pgscv-daf:latest
    container_name: pgscv-disrupt-ro
    restart: unless-stopped
    volumes:
      - ./../config/pgscv-disrupt-ro.yaml:/etc/pgscv.yaml:ro
      - ./../secrets/disrupt-ro.conn:/run/secrets/disrupt-ro.conn:ro
      - /home/daf/.ssh/id_ed25519_pgcli:/run/secrets/ssh_key:ro
    command: ["--config-file=/etc/pgscv.yaml"]
    network_mode: host
